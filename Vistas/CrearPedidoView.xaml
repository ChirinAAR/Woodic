<Window x:Class="Woodic.Vistas.CrearPedidoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generar Pedido" Height="500" Width="400" 
         WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
    FontFamily="Bahnschrift">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" 
                                              Focusable="False" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}" 
                                              RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <LinearGradientBrush.RelativeTransform>
                <TransformGroup>
                    <ScaleTransform CenterY="0.5" CenterX="0.5"/>
                    <SkewTransform CenterX="0.5" CenterY="0.5"/>
                    <RotateTransform Angle="65.225" CenterX="0.5" CenterY="0.5"/>
                    <TranslateTransform/>
                </TransformGroup>
            </LinearGradientBrush.RelativeTransform>
            <GradientStop Color="Black" Offset="1"/>
            <GradientStop Color="#FF191650" Offset="0.009"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Generar Pedido" FontSize="20" FontWeight="Bold" Foreground="#FF00C3C0" Margin="0,0,0,15"/>

            <TextBlock Text="Nombre del Cliente" Foreground="White" FontWeight="Bold"/>
            <TextBox x:Name="txtNombreCliente" Background="#FF4A4A4A" Foreground="White" Margin="0,0,0,10" ToolTip="Nombre y apellido del cliente" Padding="2" BorderBrush="Black"/>

            <TextBlock Text="Contacto" Foreground="White" FontWeight="Bold"/>
            <TextBox x:Name="txtContacto" Background="#FF4A4A4A" Foreground="White" Margin="0,0,0,10" ToolTip="Número de teléfono del cliente" Padding="2" BorderBrush="Black"/>

            <TextBlock Text="Dirección" Foreground="White" FontWeight="Bold"/>
            <TextBox x:Name="txtDireccion" Background="#FF4A4A4A" Foreground="White" Margin="0,0,0,15" ToolTip="Dirección de la casa del cliente" Padding="2" BorderBrush="Black"/>

            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Compuesto" Foreground="White" FontWeight="Bold"/>
                    <CheckBox x:Name="chkAglomerado" Content="Aglomerado" Foreground="White" Checked="chkAglomerado_Checked"/>
                    <CheckBox x:Name="chkMDF" Content="MDF" Foreground="White" Checked="chkMDF_Checked" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5,0">
                    <TextBlock Text="Línea" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbLinea" Background="#999999" Foreground="Black" SelectionChanged="cmbLinea_SelectionChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Color" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbColor" Background="#999999" Foreground="Black" DropDownOpened="cmbColor_DropDownOpened"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Cantidad de Módulos" Foreground="White" FontWeight="Bold"/>
            <TextBox x:Name="txtCantidadModulos" Background="#FF4A4A4A" Foreground="White" Margin="0,0,0,20" Width="100" HorizontalAlignment="Left" ToolTip="Número de módulos (muebles) del pedido" Padding="2" BorderBrush="Black"/>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnAceptar" Content="Aceptar" Background="#003399" Foreground="White" FontWeight="Bold" Width="100" Height="30" Margin="0,0,10,0" Click="btnAceptar_Click"/>
                <Button x:Name="btnCancelar" Content="Cancelar" Background="#660000" Foreground="White" FontWeight="Bold" Width="100" Height="30" Click="btnCancelar_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>